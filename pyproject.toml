[project]
name = "clicue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["rapidfuzz", "vosk", "sounddevice"]

[project.scripts]
clicue = "clicue.main:main"
